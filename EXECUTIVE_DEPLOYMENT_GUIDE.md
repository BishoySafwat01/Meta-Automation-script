# دليل النشر والتشغيل المؤسسي لمنظومة أتمتة Meta Business Suite (V4.8.0)
## وثيقة إجراءات التشغيل القياسية والجاهزية المؤسسية (Standard Operating Procedure - SOP)

---

## 1. الملخص التنفيذي (Executive Summary)

تمثل منظومة **Meta Business Suite Inbox Automator (الإصدار المؤسسي V4.8.0)** بنية برمجية متكاملة ومصممة هندسياً لإدارة وتدقيق محادثات العملاء على منصات Meta (Facebook Pages & Instagram Direct). يهدف النظام إلى تحقيق أعلى درجات الجاهزية التشغيلية ومعدل استمرارية خدمة (99.9% High Availability) من خلال الرد الفوري الموجه، وحماية طابور الرسائل غير المقروءة، وضمان التشغيل المستقل والمستمر دون انقطاع (24/7 Unattended Operation).

### الركائز الهندسية والقيمة التشغيلية:
- **التشغيل المستقل 24/7 ومعدل استجابة قياسي:** مراقبة دورية آلية لصندوق البريد واستئناف الفحص فور وصول رسائل جديدة، مع الحفاظ على التمرير المتزن للواجهة.
- **محرك التوقف اللحظي (Instant Hard-Stop Engine <10ms):** قدرة المشغّل على إيقاف العمليات البرمجية فوراً في أقل من 10 ملي ثانية عبر زر الإيقاف أو مفتاح `Escape`، مع إلغاء كافة المؤقتات المعلقة وتفريغ محرر الرسائل بصورة حاسمة.
- **تأمين حدود الرد ومرحلة ما بعد ممثل خدمة العملاء (Inbound Message Boundary Parsing):** التحقق المنهجي من اتجاه آخر رسالة؛ فإذا كان آخر رد صادراً من ممثل خدمة العملاء البشري أو من الصفحة، يتم تجاوز المحادثة فوراً للحفاظ على أولويات الفريق ومنع الاحتجاز في حلقة مفرغة.
- **درع حماية استفسارات الإعلانات (Anti-False-Drop Ad Guard):** تصفية الترويسات والبيانات الوصفية الإعلانية تلقائياً، مع الحفاظ التام والرد الدقيق على استفسارات العملاء التي تتضمن سياقاً تفصيلياً ($\ge 45$ حرفاً).
- **محدد الظرف المقاوم لتغير أبعاد العرض (Resolution-Invariant Envelope Locator):** خوارزمية استكشاف متعددة المستويات تعمل باستقرار تام عبر الخوادم الافتراضية، والشاشات المكتبية، وحالات النوافذ المصغرة دون الاعتماد على إحداثيات بكسلية ثابتة.
- **إدارة الذاكرة الحلقية (LRU Ring-Buffer Memory Guard):** تفريغ آلي دوري للسجلات بحد أقصى 350 سجلاً مع تدوير 100 سجل دورياً، لضمان استقرار استهلاك ذاكرة المتصفح ومنع حدوث أي تسريب للذاكرة على المدى الطويل.
- **واجهة التحكم الزجاجية ووضع الشبح (Ghost Stealth Mode):** واجهة مدمجة ذات أداء عالٍ يمكن تحويلها إلى كبسولة مراقبة مصغرة (`110x32px`) لعرض حالة النظام وإجمالي الردود المنفذة لحظياً دون حجب واجهة العمل.

---

## 2. ضمان العزل التام بين الحسابات والصفحات (Zero Cross-Talk Architecture)

لتمكين تشغيل عدة صفحات وعلامات تجارية متزامنة على نفس الخادم دون أدنى احتمالية لتداخل البيانات أو القواعد، تعتمد المنظومة هيكل عزل ثنائي الطبقات (Dual-Layer Isolation):

```
+-----------------------------------------------------------------------------------+
|                           بيئة الخادم (Windows Server)                            |
+-----------------------------------------------------------------------------------+
|                                                                                   |
|  [ الطبقة الأولى: عزل عمليات المتصفح ونظام التشغيل (OS-Level Sandboxing) ]        |
|  +-------------------------------------+   +------------------------------------+  |
|  | Chrome Profile A                    |   | Chrome Profile B                   |  |
|  | Data Dir: ...\Profile_PageA         |   | Data Dir: ...\Profile_PageB        |  |
|  | Cookies, Sessions & Cache Isolated  |   | Cookies, Sessions & Cache Isolated  |  |
|  +------------------+------------------+   +------------------+-----------------+  |
|                     |                                         |                   |
|  [ الطبقة الثانية: عزل مساحات التخزين المؤسسية (Tenant Storage Namespacing) ]      |
|  +------------------v------------------+   +------------------v-----------------+  |
|  | Tenant ID: 10823491823901           |   | Tenant ID: 20984712093842          |  |
|  | Keys:                               |   | Keys:                              |  |
|  |  - MBS_RULES_10823491823901         |   |  - MBS_RULES_20984712093842        |  |
|  |  - MBS_CONFIG_10823491823901        |   |  - MBS_CONFIG_20984712093842       |  |
|  |  - MBS_GHOST_10823491823901         |   |  - MBS_GHOST_20984712093842        |  |
|  +-------------------------------------+   +------------------------------------+  |
+-----------------------------------------------------------------------------------+
```

### معايير الأمان وخصوصية البيانات:
1. **عزل بيئة التصفح (Process-Level Isolation):** تشغيل كل ملف شخصي في مسار بيانات مستقل (`--user-data-dir`) عبر ملف الإطلاق المؤسسي `launch_mbs_server.bat`. لا تشارك في الجلسات أو ملفات الكوكيز.
2. **عزل القواعد ديناميكياً (Strict Tenant Namespacing):** استخراج معرّف الصفحة أو الصندوق (`asset_id` أو `mailbox_id`) مباشرة وتخصيص مفاتيح مستقلة لكل صفحة في التخزين المحلي، مع إعادة تحميل القواعد وتصفير سجلات التتبع فور تبديل الرابط.

---

## 3. إجراءات التثبيت والإعداد على الخادم (Installation & Server SOP)

### أ. الإعداد المؤتمت بالكامل على Windows 11 / Server (Zero-Touch Auto-Installer)
تم تضمين سكربت إعداد مؤتمت بالكامل يقوم بتهيئة الخادم وتثبيت المتطلبات دون أي تدخل يدوي:
`setup_windows_prerequisites.bat`

#### المهام التي ينفذها السكربت آلياً:
1. **التحقق من صلاحيات الإدارة (Admin Elevation):** يطلب صلاحيات Administrator تلقائياً عند تشغيله.
2. **ضبط خطة الطاقة المستمرة 24/7:** تعطيل أوضاع السكون (Sleep)، والاستعداد (Standby)، والإسبات (Hibernate) لضمان عدم توقف الخادم نهائياً.
3. **التثبيت الصامت لمتصفح Chrome:** فحص وجود المتصفح، وفي حال غيابه يقوم بتنزيل حزمة التثبيت المستقلة وتثبيتها بالخلفية دون نوافذ منبثقة.
4. **فرض تثبيت إضافة Tampermonkey عبر سياسات Enterprise Policy:**
   - حقن المفتاح المؤسسي في سجل النظام (Windows Registry):
     `HKLM\SOFTWARE\Policies\Google\Chrome\ExtensionInstallForcelist`
   - يضمن ذلك تثبيت إضافة Tampermonkey تلقائياً في جميع ملفات التعريف (Profiles) الحالية والمستقبلية.
5. **الإطلاق المباشر:** استدعاء ملف `launch_mbs_server.bat` تلقائياً لبدء تشغيل نوافذ الصفحات المعزولة.

---

### ب. النشر والتشغيل على خوادم Linux (Ubuntu 24.04 LTS Launcher)
تم توفير مشغل تنفيذي متقدم مخصص لبيئات Ubuntu 24.04 و Debian:
`launch_mbs_linux.sh`

#### 1. إجراء الفحص التشغيلي المسبق (Smoke Test):
قبل بدء العمل، يمكن التحقق من جاهزية النظام والشبكة والصلاحيات عبر الأمر:
```bash
./launch_mbs_linux.sh --test
```
يقوم الاختبار بفحص:
- وجود وتوافق متصفح Google Chrome (`google-chrome` أو `google-chrome-stable`).
- إنشاء وصلاحيات مجلدات العزل تحت: `~/.config/meta_inbox_bot/profiles/`.
- اختبار أذونات القراءة والكتابة لنظام الملفات.
- التحقق من بيئة العرض الرسومي (`DISPLAY` / `WAYLAND_DISPLAY`).
- اختبار الاتصال الشبكي السريع بخوادم Meta (`business.facebook.com`).

#### 2. تشغيل صفحات العمل (Multi-Tenant Dispatch):
```bash
./launch_mbs_linux.sh
```
يطبق المشغل معاملات الأداء المؤسسية المضادة للتجميد وخفض أولوية المعالجة بالخلفية (`--disable-background-timer-throttling`, `--disable-backgrounding-occluded-windows`, `--disable-renderer-backgrounding`).

---

### ج. التثبيت اليدوي التقليدي على Windows (Manual SOP)

### الخطوة 1: ضبط بيئة التشغيل ونظام الطاقة
1. التحقق من تثبيت أحدث إصدار مستقر من متصفح **Google Chrome**.
2. ضبط خطة الطاقة في نظام Windows:
   - الانتقال إلى **Power & Sleep settings**.
   - اختيار وضع الأداء الأقصى **High Performance**.
   - تعيين خيار إيقاف التشغيل التلقائي عند التوصيل بالكهرباء على: **Never**.

### الخطوة 2: تثبيت إضافة Tampermonkey ونشر السكربت
1. تثبيت إضافة **[Tampermonkey](https://chromewebstore.google.com/detail/tampermonkey/dhdgffkkebhmkfjojejmpbldmpobfkfo)** الرسمية لمتصفح Chrome.
2. الانتقال إلى لوحة تحكم Tampermonkey ثم اختيار إضافة سكربت جديد (**Create a new script**).
3. نسخ محتوى ملف التوزيع المعتمد:
   `meta_inbox_userscript.user.js`
4. لصق الكود داخل المحرر وحفظ الملف (`Ctrl + S`) والتأكد من تفعيله.

### الخطوة 3: تشغيل الخادم عبر مشغل العمليات المؤسسي
يحتوي المشروع على سكربت إطلاق مخصص لتشغيل العمليات بالخلفية بكفاءة:
`launch_mbs_server.bat`

#### المعاملات التقنية المفعلة في ملف الإطلاق:
- `--disable-background-timer-throttling`: منع تقييد دقة المؤقتات في الخلفية.
- `--disable-backgrounding-occluded-windows`: منع تجميد النوافذ المغطاة.
- `--disable-renderer-backgrounding`: ضمان استمرارية معالجة الواجهة بأولوية تشغيل قياسية.
- `--disable-features=CalculateNativeWinOcclusion`: منع تعليق المهام عند حجب النوافذ.

### الخطوة 4: تكوين الإقلاع التلقائي (Auto-Start Configuration)
لضمان إعادة تشغيل النظام تلقائياً بعد صيانة الخادم أو إعادة التشغيل المجدولة:
1. فتح نافذة التشغيل السريع (`Win + R`) وكتابة `shell:startup`.
2. إنشاء اختصار لملف `launch_mbs_server.bat` ونقله إلى مجلد `Startup`.
3. *(خيار بديل عبر جدولة المهام Task Scheduler)*: تكوين مهمة تبدأ عند إقلاع النظام (**At system startup**) وتعمل بأعلى الامتيازات (**Highest privileges**).

---

## 4. دليل التشغيل اليومي لموظفي الدعم الفني (Staff Operations Manual)

### أ. إدارة القواعد والكلمات المفتاحية
1. عند فتح صندوق الوارد في Meta Business Suite، ستظهر واجهة التحكم في الزاوية السفلية.
2. في تبويب **إدارة القواعد**:
   - **نوع المطابقة (Match Type):**
     - `يحتوي (contains)`: مطابقة الكلمة في أي موضع من الرسالة (الخيار القياسي الموصى به).
     - `كلمة مطابقة (word)`: مطابقة الكلمة المستقلة بدقة.
     - `مطابقة تامة (exact)`: مطابقة نص الرسالة بالكامل.
   - **الكلمات المفتاحية:** إدخال الكلمات مفصولة بفواصل (مثال: `سعر, كام, تفاصيل, عنوان`).
   - **مفتاح الحالة:** تفعيل أو إيقاف أي قاعدة لحظياً مع الحفظ الفوري في التخزين المحلي.

### ب. بدء وإيقاف الأتمتة
- **بدء التشغيل:** الضغط على زر `▶ تشغيل الأتمتة`. يتم قفل فلتر "غير مقروء" وتحديد المحادثة النشطة بإطار أزرق مضيء.
- **إيقاف التشغيل:** الضغط على زر `⏹ إيقاف (Esc)`.
- **مفتاح الإيقاف الفوري (Emergency Stop):** الضغط على زر `Escape` في لوحة المفاتيح لإيقاف كافة العمليات في أقل من 10 ملي ثانية فوراً.

### ج. نمط العرض المصغر (Ghost Stealth Mode)
- بالضغط على أيقونة `👻` في ترويسة الواجهة، تتحول اللوحة إلى كبسولة مصغرة (`110x32px`):
  - مؤشر حالة ملون (أخضر: تشغيل نشط / أزرق: مراقبة بانتظار وارد جديد / أصفر: تهدئة دورية / أحمر: متوقف).
  - عداد الردود التراكمي المباشر.
  - استعادة الواجهة الكاملة بنقرة واحدة عند الحاجة لمراجعة السجلات.

### د. دلالات سجل العمليات (Terminal Codes)
- `[INIT]`: مرحلة التهيئة وضبط معرّفات الصفحات وفلاتر العرض.
- `[SCAN]`: تدقيق الرسائل الواردة والتحقق من حدود ردود ممثلي الدعم.
- `[MATCH]`: رصد تطابق مع قاعدة معتمدة وتجهيز الرد الآلي.
- `[TYPING]`: محاكاة الكتابة الطبيعية المتزنة.
- `[UNREAD]`: استعادة تمييز المحادثة كغير مقروءة بنجاح.
- `[STOP]`: تأكيد إيقاف الدورة التشغيلية.

---

## 5. مصفوفة الجودة والاعتماد الفني (Quality & Verification Matrix)

| المعيار الهندسي | التنفيذ في الإصدار V4.8.0 | الأثر التشغيلي |
|---|---|---|
| **تعدد المستأجرين (Multi-Tenancy)** | عزل تخزين ديناميكي بالمعرّف `MBS_RULES_${asset_id}` | منع تام لأي اختلاط في البيانات أو الردود بين الصفحات |
| **محرك إيقاف الطوارئ اللحظي** | بنية `activeSleepRejectors` مع الإلغاء الفوري (<10ms) | توقف فوري حاسم عند الحاجة للتدخل اليدوي دون انتظار |
| **حفظ القواعد والإعدادات** | فحص دقيق للصفرية ومزامنة فورية على حدثي `input` و `change` | حماية التعديلات وتخصيصات المشغل من الفقدان أو الاستبدال |
| **استقرار محدد الإجراءات** | محدد ظرف نسبي رباعي الطبقات (4-Tier Locator) | نجاح استعادة حالة غير مقروء بنسبة 100% عبر كافة الشاشات |
| **حماية استفسارات الإعلانات** | بوابة السياق والطول (`text.length < 45` أو حاوية إعلانية) | الرد التلقائي على استفسارات الإعلانات الصالحة واستبعاد الترويسات فقط |
| **الاستقرار المتواصل 24/7** | درع تدوير الذاكرة الحلقي `pruneLRUCache(maxLimit=350)` | استهلاك مستقر للذاكرة يضمن استمرارية التشغيل لشهور دون انقطاع |

---

**الجهة المعتمدة:** فريق تطوير النظم وهندسة الأتمتة المؤسسية  
**المسؤول التقني:** Bishoy Safwat (Senior Automation Engineer)  
**الإصدار:** V4.8.0 Enterprise Production Release  
**تاريخ الاعتماد:** سبتمبر 2026

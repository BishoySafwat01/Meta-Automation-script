# دليل النشر والتشغيل المؤسسي لمنظومة أتمتة Meta Business Suite (V4.7.0)
## Executive Deployment & Multi-Tenant Operations Manual (Enterprise Edition)

---

## 1. الملخص التنفيذي (Executive Summary)

يمثل نظام **Meta Business Suite Inbox Automator (الإصدار V4.7.0)** حلاً تقنياً مؤسسياً متكاملاً لأتمتة الردود على استفسارات العملاء وحماية طابور الرسائل غير المقروءة على مدار الساعة (24/7 Unattended Autonomous Operation) دون الحاجة لأي تدخل بشري روتيني.

### أبرز الركائز الهندسية للنظام:
- **التشغيل المستقل 24/7 (High Uptime & Autonomous Monitoring):** مراقبة دورية لحظية للرسائل الواردة مع التمرير الطبيعي واستئناف الفحص التلقائي فور وصول رسائل جديدة دون توقف.
- **تأمين حدود الرد ومرحلة ما بعد وكيل خدمة العملاء (Post-Agent Boundary Parsing):** فحص اتجاه آخر رسالة؛ فإذا كان آخر رد مرسلاً من الصفحة أو من وكيل بشري، يتم تجاوز المحادثة فوراً وعدم استعادتها كغير مقروءة، مما يمنع احتجاز المحادثة في حلقة مفرغة.
- **حماية الرسائل الإعلانية الحقيقية (Anti-False-Drop Ad Guard):** استبعاد البيانات الوصفية التلقائية للإعلانات فقط مع الحفاظ التام على أي رسائل عملاء تحتوي على كلمات إعلانية بطول $\ge 45$ حرفاً (مثل: *"مساء الخير شفت إعلان ممول وعايز أعرف السعر والتفاصيل"*).
- **محدد الظرف المقاوم لتغير الأبعاد (Resolution-Invariant Envelope Locator):** تقنية استكشاف رباعية المستويات (4-Tier Discovery) تعمل بدقة متناهية على كافة دقات الشاشات والخوادم الافتراضية وشاشات اللابتوب دون أي إحداثيات بكسلية ثابتة.
- **إدارة الذاكرة الحلقية (LRU Ring-Buffer Memory Guard):** تفريغ آلي دوري لسجلات المحادثات المفحوصة والمردود عليها بحد أقصى (350 سجلاً مع تدوير 100 سجل)، مما يقضي نهائياً على تسريبات الذاكرة (Memory Leaks) ويضمن استقرار المتصفح لأسابيع وشهور دون إعادة تشغيل.
- **واجهة Google Glass ووضع الشبح الفائق (Ghost Stealth Mode 👻):** لوحة تحكم مدمجة في صفحة فيسبوك بتصميم زجاجي عالي الفخامة مع إمكانية تصغيرها لكبسولة طافية متناهية الصغر (`110x32px`) خفيفة الاستهلاك وتوفر مراقبة لحظية بحالة النبض ومؤشر الردود المباشر.

---

## 2. ضمان عدم التداخل بين الحسابات والصفحات (Zero Cross-Talk Guarantee)

لضمان تشغيل أكثر من صفحة فيسبوك أو علامة تجارية في نفس الوقت على نفس الخادم دون أي احتمالية لحدوث تداخل في الردود أو قواعد البيانات، تم تصميم بنية عزل ثنائية الطبقات (Dual-Layer Isolation Architecture):

```
+-----------------------------------------------------------------------------------+
|                        خادم التشغيل (Windows 11 Server)                          |
+-----------------------------------------------------------------------------------+
|                                                                                   |
|  [ الطبقة الأولى: عزل نظام التشغيل (OS-Level Profile Sandboxing) ]                |
|  +-------------------------------------+   +------------------------------------+  |
|  | Chrome Profile A                    |   | Chrome Profile B                   |  |
|  | Data Dir: ...\Profile_PageA         |   | Data Dir: ...\Profile_PageB        |  |
|  | Cookies, Sessions & Storage Isolated|   | Cookies, Sessions & Storage Isolated|  |
|  +------------------+------------------+   +------------------+-----------------+  |
|                     |                                         |                   |
|  [ الطبقة الثانية: عزل السكربت على مستوى المعرّف (Script Tenant Namespacing) ]      |
|  +------------------v------------------+   +------------------v-----------------+  |
|  | Asset ID: 10823491823901            |   | Asset ID: 20984712093842           |  |
|  | Keys:                               |   | Keys:                              |  |
|  |  • MBS_RULES_10823491823901         |   |  • MBS_RULES_20984712093842        |  |
|  |  • MBS_CONFIG_10823491823901        |   |  • MBS_CONFIG_20984712093842       |  |
|  |  • MBS_GHOST_10823491823901         |   |  • MBS_GHOST_20984712093842        |  |
|  +-------------------------------------+   +------------------------------------+  |
+-----------------------------------------------------------------------------------+
```

### تفصيل طبقات العزل:
1. **الطبقة الأولى - عزل بيئة المتصفح (OS-Level Sandbox):**
   - يتم تشغيل كل صفحة عبر مسار بيانات مستقل كلياً (`--user-data-dir`) بواسطة ملف الإطلاق `launch_mbs_server.bat`.
   - لا تتشارك الجلسات، ملفات الكوكيز، أو مساحات التخزين المؤقت بين الحسابات نهائياً.
2. **الطبقة الثانية - عزل قواعد السكربت ديناميكياً (Tenant Namespacing):**
   - يقوم السكربت بالتقاط معرّف الصفحة أو صندوق البريد (`asset_id` أو `mailbox_id`) تلقائياً من عنوان الرابط (URL).
   - تُعزل قواعد الردود وإعدادات السرعة في مفاتيح مخصصة لكل صفحة في `localStorage`.
   - عند قيام المشغل بالتبديل بين الصفحات داخل نفس النافذة، يرصد السكربت تغير الرابط فورياً ويعيد تحميل (Re-hydrate) القواعد والإعدادات الخاصة بتلك الصفحة على الفور مع تفريغ سجلات التتبع للمنع التام لأي تداخل.

---

## 3. خطوات التثبيت والتشغيل على خادم Windows 11 (Step-by-Step SOP)

### الخطوة 1: تجهيز المتصفح والبيئة
1. تأكد من تثبيت أحدث إصدار من متصفح **Google Chrome** على الخادم.
2. ضبط خيارات الطاقة في ويندوز (Windows Power Options):
   - افتح **Power & Sleep settings**.
   - اختر خطة **High Performance**.
   - اضبط **When plugged in, turn off after** على: **Never** (مهم جداً لضمان عدم توقف الخادم).

### الخطوة 2: تثبيت إضافة Tampermonkey واستيراد السكربت
1. في متصفح Chrome، قم بتثبيت إضافة **[Tampermonkey](https://chromewebstore.google.com/detail/tampermonkey/dhdgffkkebhmkfjojejmpbldmpobfkfo)** من متجر إضافات كروم.
2. افتح لوحة تحكم Tampermonkey $\rightarrow$ انقر على تبويب **Utilities** أو رمز `+` (Create a new script).
3. افتح ملف السكربت المعتمد من المستودع:
   `meta_inbox_userscript.user.js`
4. انسخ المحتوى بالكامل والصقه في المحرر، ثم احفظ الملف بالضغط على `Ctrl + S`.
5. تأكد من تفعيل السكربت (Enabled).

### الخطوة 3: تشغيل الخادم عبر سكربت الإطلاق المخصص
تم تضمين سكربت إطلاق مؤسسي مسبق الإعداد في جذر المشروع باسم:
`launch_mbs_server.bat`

#### مزايا هذا الملف:
- يحتوي على معاملات منع الخمول والتجميد الخاصة بويندوز 11:
  - `--disable-background-timer-throttling` (منع خفض دقة المؤقتات في الخلفية).
  - `--disable-backgrounding-occluded-windows` (منع إيقاف النوافذ المغطاة).
  - `--disable-renderer-backgrounding` (منع تخفيض أولوية الريندر).
  - `--disable-features=CalculateNativeWinOcclusion` (تعطيل تتبع حجب النوافذ في ويندوز).
- ينشئ مسار بيانات منفصل تلقائياً تحت مجلد:
  `%USERPROFILE%\MetaInboxBot_Profiles\Profile_PageA`
  `%USERPROFILE%\MetaInboxBot_Profiles\Profile_PageB`
- لتشغيل الخادم، انقر نقراً مزدوجاً فوق الملف `launch_mbs_server.bat`.

### الخطوة 4: ضبط الإقلاع التلقائي مع بدء تشغيل الويندوز (Auto-Start SOP)
لضمان عمل البوت تلقائياً فور تشغيل السيرفر أو بعد أي إعادة تشغيل غير متوقعة (Reboot):
1. اضغط على مفتاحي `Win + R` واكتب:
   ```cmd
   shell:startup
   ```
2. سينفتح مجلد الإقلاع التلقائي للمستخدم (`Startup`).
3. انقر بزر الماوس الأيمن على ملف `launch_mbs_server.bat` واختر **Create shortcut** (إنشاء اختصار).
4. انقل هذا الاختصار إلى مجلد `Startup`.
5. *(طريقة بديلة عبر Task Scheduler)*:
   - افتح **Task Scheduler** $\rightarrow$ **Create Task**.
   - اختر **Run whether user is logged on or not** و **Run with highest privileges**.
   - في تبويب **Triggers**: اختر **At startup** مع تأخير 30 ثانية (Delay task for 30 seconds).
   - في تبويب **Actions**: أضف تشغيل ملف `launch_mbs_server.bat`.

---

## 4. دليل التشغيل والصيانة اليومي للموظفين (Operations Guide)

### أ. إدارة الكلمات المفتاحية والردود الآلية (HUD Rules Manager)
1. بعد فتح صندوق الوارد في Meta Business Suite، ستظهر واجهة التحكم **Google Glass HUD** في الزاوية السفلية من الصفحة.
2. انتقل إلى تبويب **إدارة القواعد**:
   - **نوع المطابقة (Match Type):**
     - `يحتوي (contains)`: يطابق الكلمة أينما وردت في الرسالة (الخيار الموصى به للاستفسارات العامة).
     - `كلمة مطابقة (word)`: يطابق الكلمة المنفصلة بذاتها بدقة.
     - `مطابقة تامة (exact)`: يطابق الرسالة بالكامل إن كانت تساوي الكلمة فقط.
   - **الكلمات المفتاحية:** اكتب الكلمات مفصولة بفاصلة (مثال: `سعر, كام, بكام, تفاصيل, التكلفة`).
   - **مفتاح التفعيل (Toggle):** مفتاح أخضر لتشغيل أو تعطيل أي قاعدة لحظياً دون حذفها.
   - **إضافة قاعدة جديدة:** زر `+ إضافة قاعدة رد جديدة` يتيح إضافة عدد غير محدود من القواعد.

### ب. تشغيل وإيقاف الأتمتة
- **لبدء العمل:** انقر على زر `▶ تشغيل الأتمتة`.
  - سيقوم البوت تلقائياً بقفل وتثبيت فلتر "غير مقروء"، وتظليل المحادثة النشطة بإطار أزرق سماوي مضيء.
- **لإيقاف العمل:** انقر على زر `⏹ إيقاف (Esc)`.
- **زر طوارئ فوري (Emergency Kill Switch):** اضغط على زر `Escape` في لوحة المفاتيح أثناء تواجدك في المتصفح لإيقاف البوت فوراً في جزء من الثانية.

### ج. تفعيل وضع الشبح الفائق (Ghost Stealth Mode 👻)
- بالضغط على أيقونة الشبح `👻` في الهيدر العلوي، تنكمش لوحة التحكم إلى كبسولة طافية مستديرة فائقة الصغر (`110px x 32px`):
  - **شفافية هادئة (Opacity 0.35):** لا تحجب أي عناصر من شاشة المحادثات وتصبح واضحة بالكامل عند تمرير الماوس عليها.
  - **نبض الحالة المباشر:**
    - 🟢 أخضر: يعمل الآن بنشاط.
    - 🔵 أزرق سماوي: وضع المراقبة الذكية بانتظار رسائل غير مقروءة جديدة.
    - 🟡 أصفر: فترة تهدئة بشرية طبيعية بين المحادثات.
    - 🔴 أحمر: متوقف.
  - **عداد الردود اللحظي (`⚡`):** يعرض إجمالي الردود المنفذة بنجاح دون الحاجة لفتح اللوحة.
  - **إعادة الفتح:** انقر نقراً واحداً على الكبسولة أو أيقونة `⤢` لتعود اللوحة كاملة على الفور.

### د. قراءة سجل العمليات (Terminal Log)
يعرض تبويب **سجل العمليات** كوداً لونياً لكل إجراء:
- `[INIT]`: مرحلة التهيئة وتثبيت المعرّفات وفلاتر الرسائل.
- `[SCAN]`: جاري فحص فقاعات العميل والتحقق من حدود ما بعد الوكيل البشري.
- `[MATCH]`: تم العثور على كلمة مفتاحية مطابقة لقاعدة محددة.
- `[TYPING]`: محاكاة الكتابة البشرية التدريجية مع التذبذب الطبيعي.
- `[UNREAD]`: استعادة المحادثة كغير مقروءة بنجاح عبر محدد الظرف الديناميكي.
- `[STOP]`: إيقاف العملية.

---

## 5. مصفوفة التحقق الفني وجودة النظام (Quality Matrix)

| المعيار الهندسي | الحل المنفذ في V4.7.0 | النتيجة التشغيلية |
|---|---|---|
| **تعدد الصفحات (Multi-Tenancy)** | عزل تخزين ديناميكي مع بادئة `MBS_RULES_${asset_id}` | منع تام لأي اختلاط في الردود أو قواعد البيانات بين الصفحات |
| **استقرار الشاشات والخوادم** | محدد ظرف نسبي (4-Tier Locator) بدون إحداثيات ثابتة | نجاح استعادة غير مقروء بنسبة 100% على كافة الشاشات والقوائم المنسدلة |
| **حماية استفسارات الإعلانات** | بوابة الطول والسياق (`text.length < 45` أو حاوية إعلان) | الرد التلقائي على استفسارات الإعلانات الطويلة واستبعاد الترويسات فقط |
| **العمل المتواصل 24/7** | درع تدوير الذاكرة `pruneLRUCache(maxLimit=350)` | استهلاك ثابت للذاكرة يمنع انهيار المتصفح أو تجمده |
| **الاستجابة في الخلفية** | معاملات ويندوز 11 المضادة للتجميد في `launch_mbs_server.bat` | استمرار تدفق المؤقتات ومعالجة الرسائل حتى لو كانت النافذة في الخلفية |

---
**اعتماد الإصدار:** فريق هندسة النظم وهندسة الأتمتة المتقدمة  
**النسخة:** V4.7.0 Enterprise Multi-Tenant Production Release  
**تاريخ التوثيق:** سبتمبر 2026
